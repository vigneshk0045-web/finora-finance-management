import { useState } from "react"; import api from "../api/client";
export default function Contact(){ const [form,setForm]=useState({name:'',email:'',subject:'',message:''}); const [msg,setMsg]=useState('');
 const submit=async(e)=>{e.preventDefault(); try{ const {data}=await api.post('/api/contact',form); setMsg(data.message); setForm({name:'',email:'',subject:'',message:''}); }catch(err){ setMsg(err.response?.data?.message || 'Could not send message'); }};
 return <div className="card form-card"><div className="section-header"><div><div className="eyebrow">Contact</div><h2>Talk to the Finora team</h2><p>Use this page as your public support and sales touchpoint.</p></div></div><form className="form-grid" onSubmit={submit}><input placeholder="Name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><input placeholder="Email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><input className="full-span" placeholder="Subject" value={form.subject} onChange={e=>setForm({...form,subject:e.target.value})}/><textarea className="full-span" rows="6" placeholder="Message" value={form.message} onChange={e=>setForm({...form,message:e.target.value})}/><button className="primary-btn" type="submit">Send message</button>{msg && <div className="form-message">{msg}</div>}</form></div>; }
