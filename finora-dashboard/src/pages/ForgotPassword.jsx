import { useState } from "react"; import api from "../api/client";
export default function ForgotPassword(){ const [email,setEmail]=useState(''); const [msg,setMsg]=useState(''); const submit=async(e)=>{e.preventDefault(); const {data}=await api.post('/api/auth/forgot-password',{email}); setMsg(data.message);}; return <div className="auth-wrap"><div className="card auth-card"><div className="eyebrow">Password help</div><h2>Forgot password</h2><form className="form-grid" onSubmit={submit}><input placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><button className="primary-btn full">Send instructions</button>{msg && <div className="form-message">{msg}</div>}</form></div></div>; }
